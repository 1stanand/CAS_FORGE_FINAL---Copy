{
  "WorkFlow": [
    "@BulkAllocationGrid",
    "@CMSModule",
    "@APIServices",
    "@Proposal and not @MoveToNext",
    "@Proposal and @MoveToNext",
    "@LeadDetails and @OpenApplication",
    "@LeadDetails and @AppInfo and @Primary",
    "@LeadDetails and @AppInfo and @CoApplicant",
    "@LeadDetails and @AppInfo and @Guarantor",
    "@LeadDetails and @LoanDetails",
    "@LeadDetails and not @MoveToNext",
    "@LeadDetails and @MoveToNext",
    "@CCDE and @OpenApplication",
    "@CCDE and @AppInfo and @Primary",
    "@CCDE and @AppInfo and @AddOnCard",
    "@CCDE and @AppInfo and @Guarantor",
    "@CCDE and @LoanDetails",
    "@CCDE and not @MoveToNext and not @Enquiry",
    "@CCDE and @Enquiry",
    "@CCDE and @MoveToNext",
    "@LoginAcceptance and @OpenApplication",
    "@LoginAcceptance and not @MoveToNext and not @Reject",
    "@LoginAcceptance and @Reject",
    "@LoginAcceptance and @MoveToNext",
    "@KYC and @OpenApplication",
    "@KYC and not @MoveToNext and not @Reject",
    "@KYC and @Reject",
    "@KYC and @MoveToNext",
    "@CreditBureauReferral and @OpenApplication",
    "@CreditBureauReferral and not @MoveToNext and not @Reject",
    "@CreditBureauReferral and @Reject",
    "@CreditBureauReferral and @MoveToNext",
    "@DDE and @PickApplication",
    "@DDE and @OpenApplication",
    "@DDE and @AppInfo and @Primary",
    "@DDE and @AppInfo and @CoApplicant",
    "@DDE and @AppInfo and @Guarantor",
    "@DDE and @LoanDetails",
    "@DDE and @DealAndFacilityDetails",
    "@Facility",
    "@DDE and @Document",
    "@DDE and @Collateral",
    "@DDE and @RepaymentInstrument",
    "@DDE and @ContractualCharge",
    "@DDE and @Charge",
    "@DDE and @PeriodicCharge",
    "@DDE and not @MoveToNext",
    "@DDE and @MoveToNext",
    "@DDEQC",
    "@DDEQC and @MoveToNext",
    "@GoldValuation and @OpenApplication",
    "@GoldValuation and not @MoveToNext",
    "@GoldValuation and @MoveToNext",
    "@PreSanctionVisit and @OpenApplication",
    "@PreSanctionVisit and @AppInfo and @Primary",
    "@PreSanctionVisit and @AppInfo and @CoApplicant",
    "@PreSanctionVisit and @AppInfo and @Guarantor",
    "@PreSanctionVisit and @GroupInformation",
    "@PreSanctionVisit and not @MoveToNext and not @DocumentApproval",
    "@PreSanctionVisit and @MoveToNext",
    "@PreSanctionVisit and @DocumentApproval",
    "@Recommendation and @OpenApplication",
    "@Recommendation and @Reject",
    "@Recommendation and @Collateral",
    "@Recommendation and @AppInfo and @Primary",
    "@Recommendation and @AppInfo and @CoApplicant",
    "@Recommendation and @AppInfo and @Guarantor",
    "@Recommendation and @GroupInformation",
    "@Recommendation and not @MoveToNext",
    "@Recommendation and @MoveToNext",
    "@CII and @OpenApplication",
    "@CII and not @MoveToNext",
    "@CII and @MoveToNext",
    "@CIV and @OpenApplication",
    "@CIV and not @MoveToNext",
    "@CIV and @MoveToNext",
    "@CIC and @OpenApplication",
    "@CIC and not @MoveToNext",
    "@CIC and @MoveToNext",
    "@FII and @OpenApplication",
    "@FII and not @MoveToNext",
    "@FII and @MoveToNext",
    "@FIV and @OpenApplication",
    "@FIV and not @MoveToNext",
    "@FIV and @MoveToNext",
    "@FIC and @OpenApplication",
    "@FIC and not @MoveToNext",
    "@FIC and @MoveToNext",
    "@AccountUpdation and @GroupInformation",
    "@AccountUpdation and @MoveToNext",
    "@CreditApproval and @PickApplication",
    "@CreditApproval and @OpenApplication",
    "@CreditApproval and @Collateral",
    "@CreditApproval and @ContractualCharge",
    "@CreditApproval and @AppInfo and @Primary",
    "@CreditApproval and @AppInfo and @CoApplicant",
    "@CreditApproval and @AppInfo and @Guarantor",
    "@CreditApproval and @CommitteeInitiation",
    "@CreditApproval and @CommitteeApproval",
    "@CreditApproval and @CommitteeApproval and @ApplicationManager",
    "@CreditApproval and not @MoveToNext and not @DocumentApproval and not @Reject and not @ICD",
    "@CreditApproval and @Reject",
    "@CreditApproval and @MoveToNext and not @ICD",
    "@CreditApproval and @DocumentApproval",
    "@ICD and @LoanDetails",
    "@ICD and @MoveToNext and not @CreditApproval",
    "@ICD and @CreditApproval and not @MoveToNext",
    "@ICD and @CreditApproval and @MoveToNext",
    "@FDE and @OpenApplication",
    "@FDE",
    "@FDE and @MoveToNext",
    "@AssetValidation and @OpenApplication",
    "@AssetValidation and not @MoveToNext",
    "@AssetValidation and @MoveToNext",
    "@Reconsideration and @OpenApplication",
    "@Reconsideration and @Collateral",
    "@Reconsideration and not @MoveToNext",
    "@Reconsideration and @MoveToNext",
    "@PostApproval and @PickApplication",
    "@PostApproval and @OpenApplication",
    "@PostApproval and @Document",
    "@PostApproval and @ContractualCharge",
    "@PostApproval and @RepaymentInstrument",
    "@PostApproval and not @MoveToNext",
    "@PostApproval and @MoveToNext",
    "@FacilityInitiation and @OpenApplication",
    "@FacilityInitiation",
    "@FacilityInitiation and @MoveToNext",
    "@RcuInitiation",
    "@RcuReferral",
    "@OperationCheck and @OpenApplication",
    "@OperationCheck and not @MoveToNext",
    "@OperationCheck and @MoveToNext",
    "@TrancheInitiation and @OpenApplication",
    "@TrancheInitiation and not @MoveToNext",
    "@TrancheApproval and @OpenApplication",
    "@TrancheApproval and not @MoveToNext",
    "@TrancheApproval and @MoveToNext",
    "@Disbursal and @PickApplication",
    "@Disbursal and @OpenApplication",
    "@Disbursal and @Reject",
    "@Disbursal and @Collateral",
    "@Disbursal and  @ContractualCharge",
    "@Disbursal and @Charge",
    "@Disbursal and @PeriodicCharge",
    "@Disbursal and @RepaymentInstrument",
    "@Disbursal and not @MoveToNext",
    "@Disbursal and @MoveToNext",
    "@DisbursalAuthor and @OpenApplication",
    "@DisbursalAuthor and @RepaymentInstrument",
    "@DisbursalAuthor",
    "@MultipleDisbursal",
    "@TrancheInitiation and @MoveToNext",
    "@DCCScheduling and @OpenApplication",
    "@DCCScheduling and @LoanDetails",
    "@DCCScheduling and @GroupInformation",
    "@DCCScheduling and @Disbursal",
    "@DCCScheduling and not @MoveToNext",
    "@DCCScheduling and @MoveToNext",
    "@DCCExecution and not @MoveToNext",
    "@DCCExecution and @MoveToNext",
    "@SentToOps and @OpenApplication",
    "@SentToOps",
    "@AppUpdateApproval and @PickApplication",
    "@AppUpdateApproval and @CommitteeInitiation",
    "@AppUpdateApproval and @CommitteeInitiation and @ApplicationManager",
    "@CardManagement and not @Enquiry",
    "@CardManagement and @Enquiry",
    "@LMS and @OpenApplication",
    "@LMS",
    "@LMS and @ContractualCharge",
    "@LMS and @RepaymentInstrument",
    "@Enquiry"
  ]
}
